<div class="container mx-auto pt-3">
  <!-- 投稿一覧 -->
  <div class="flex flex-wrap px-4">
    <% if @posts.present? %>
      <%= render @posts %>
    <% else %>
      <div class="w-full text-center mb-3">投稿がありません</div>
    <% end %>
  </div>
</div>
